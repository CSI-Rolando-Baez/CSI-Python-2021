import pygame
# Initializes the code
pygame.init()
#This line of code serves to create the display for the snake game. The integers serve as the pixel size of the window opened.
dis=pygame.display.set_mode((400,300))
# Update will update the screen as set_mode has described
pygame.display.update()
# Unitializes the code
pygame.quit()
quit()